import { View, Text ,Button,Image} from 'react-native'
import React from 'react'
import {Card} from 'react-native-shadow-cards';

export default function Cards({card_style}) {
  return (
   
    <View >
    <Card style={card_style}>
    
    </Card>
   
  </View>

  );
}